import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  pagination: {
    margin: '15px 10px',
  },
  paginationContainer: {
    margin: '15px 10px',
    display: 'flex',
    justifyContent: 'center',
  },
  list: {
    minHeight: '60vh',
  },
});
